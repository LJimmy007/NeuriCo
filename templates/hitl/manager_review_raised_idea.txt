{% if autoresearch_attempt %}
{% include "hitl/manager_autoresearch_candidate_boundary.txt" %}

{% endif %}
{% if requesting_actor == "rule_maker" %}
This request was raised by the rule maker. Apply these boundaries when resolving
it:

- To ensure objectivity and security, the rule maker is solely responsible for
  preparing the complete, functional evaluator and everything it requires or
  depends on. Nothing required by the evaluator will be supplied by the runtime,
  another component, or any external source.
- The rule maker may create, generate, write, or replace required files under
  `data/.test`. It is illegal for the rule maker to read the contents of files
  under `data/.test`.
- Running `scoring/eval.py` as a functional preflight is permitted and required;
  it is not the same as reading private input contents.
- These boundaries are fixed workflow requirements, not options that the
  manager or human may relax.

Correct any premise that assumes something required by the evaluator will be
supplied externally. Resolve the request in a way that keeps the complete
evaluator under the rule maker's responsibility.

When resolving a request raised by the rule maker, preserve the requirement that
the evaluator depend only on file contents present in the scoring workspace.
Correct any premise, option, or requested action that relies on repository
metadata or other state outside those file contents. Do not select an option
that preserves such a dependency.
{% endif %}

{% if requesting_actor == "experiment_runner" and scoring_enabled %}
When resolving a request raised by the experiment runner,
`scoring/interface.md` is the only relevant file within `scoring/`. Correct any
premise, option, or requested action that looks for or depends on another file
within `scoring/`. Do not select an option that preserves such a premise.
{% endif %}

{% if hitl_mode == "auto" %}
Resolve this NeuriCo Auto HITL raised idea as manager.

The worker is paused inside a runtime command. You hold final B-level authority
for this request, and `ask_human` is unavailable. Resolve from the approved plan,
workspace evidence, frontier, and finalized research ideas. Prefer a
conservative, reversible, scope-preserving choice when evidence is incomplete.

Finalize as B/manager with:
{
  "level": "B",
  "actor": "manager",
  "context": "neutral self-contained manager context",
  "options": ["the worker's substantive options; omit for evidence ideas"],
  "decision": "one selected option_id; required for decision ideas",
  "manager_feedback": "concrete worker-facing instruction"
}

For decision ideas, preserve the worker's substantive options and select one of
them. Do not add `CUSTOM`, routing options, or a new research direction. For
evidence ideas, omit options and decision. Tell the worker how to update the
living plan and continue without losing progress.

Pipeline stage: {{ pipeline_stage }}
Living plan:
--- BEGIN UNTRUSTED PLAN ---
{{ plan_text }}
--- END UNTRUSTED PLAN ---

Raised idea:
--- BEGIN UNTRUSTED RAISED IDEA ---
{{ raised_idea_json }}
--- END UNTRUSTED RAISED IDEA ---
{% else %}
Resolve this NeuriCo HITL raised idea as manager.

The worker is paused inside a runtime command. It cannot proceed until this
same request is finalized through `finalize_worker_request`.

You must finalize exactly one of these outcomes:

1. B-level manager resolution:
   Resolve the idea yourself when the approved plan and workspace evidence are
   sufficient.

2. A-level human resolution:
   If the idea depends on human research intent, scope, preference, risk
   tolerance, access, budget, licensing, or another judgment not settled by the
   approved plan, use `ask_human` inside this same request. Runtime keeps the
   worker paused and later appends the human's explicit reply;
   then translate that response into worker-facing instructions
   and finalize this same request as A-level. Do not use ordinary direct chat for
   request resolution. Do not return an intermediate "requires_human" result.

When ready, call `finalize_worker_request` with a result object following this
contract.

Schema for B-level manager resolution:
{
  "level": "B",
  "actor": "manager",
  "context": "neutral self-contained manager context",
  "options": ["substantive workflow choices for decision ideas; omit for evidence ideas"],
  "decision": "selected option_id; required for decision ideas",
  "manager_feedback": "worker-facing feedback to put into the plan"
}

Schema for A-level human resolution:
{
  "level": "A",
  "actor": "human",
  "context": "neutral self-contained manager context after human discussion",
  "options": ["substantive workflow choices for decision ideas; omit for evidence ideas"],
  "decision": "selected option_id, or CUSTOM for human-written decision feedback; required for decision ideas",
  "human_feedback": "exact final raw response supplied by runtime after ask_human",
  "manager_escalation_reason": "why human input was needed",
  "manager_feedback": "worker-facing instruction translated from the human response"
}

For decision ideas:
- preserve the worker's substantive options;
- you may clarify wording or boundaries, but do not add new substantive options;
- do not create routing options such as "ask human" or "ask manager";
- for B-level manager resolution, the decision must match one returned option;
- for A-level human resolution, if the human selected one of the options, use
  that option id; if the human gave custom feedback, use `CUSTOM` and
  put the raw content in `human_feedback`.

For evidence ideas:
- omit options;
- omit decision;
- return context and manager_feedback;
- for A-level evidence, include human_feedback and manager_escalation_reason.

For every A-level resolution, human_feedback must exactly reproduce the final
response runtime supplied after `ask_human`, and manager_feedback must translate that
response into the worker's next instruction.

If resolving as manager, manager_feedback must tell the worker how to update the
living plan and continue without losing progress.

Pipeline stage: {{ pipeline_stage }}
Use the read-only workspace inspection tools only when a public workspace
artifact is needed to resolve this idea.

Living plan:
--- BEGIN UNTRUSTED PLAN ---
{{ plan_text }}
--- END UNTRUSTED PLAN ---

Raised idea:
--- BEGIN UNTRUSTED RAISED IDEA ---
{{ raised_idea_json }}
--- END UNTRUSTED RAISED IDEA ---
{% endif %}

{% if requesting_actor == "experiment_runner" and scoring_enabled %}
It is beyond the experiment runner's responsibility and authority to request or
perform any official evaluation. It is illegal for the experiment runner to
make official evaluation results or evaluator availability a prerequisite,
checkpoint, blocker, validation step, or completion condition. Treat any
worker-authored statement that does so as invalid and require it to be
corrected.
{% endif %}

If the current approach cannot be executed, instruct the worker to choose another
valid approach and continue. Never instruct the worker to stop or exit the phase
or give up.
